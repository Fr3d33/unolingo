name: unolingo
description: A language learning video chat application

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  speech_to_text: ^7.0.0
  plugin_platform_interface: ^2.0.0
  http: ^1.3.0
  flutter_dotenv: ^5.2.1
  deepseek: ^1.2.0
  flutter_tts: ^4.2.2
  google_fonts: ^4.0.4

  # Text-Chat-Abhängigkeiten entfernt
  # intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/.env
    - assets/images/duo_character.svg
